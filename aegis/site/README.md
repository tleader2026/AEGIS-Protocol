# AEGIS Site

The public developer portal and protocol presentation for AEGIS.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The site renders protocol content from `data/protocol.ts` and should stay in sync with `/aegis/docs`, `/aegis/rfcs`, and `/aegis/schemas`.
